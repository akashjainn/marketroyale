{
  "buildCommand": "pnpm -w run build:all",
  "outputDirectory": "dist",
  "functions": {
    "api/[...all].ts": {
      "includeFiles": "dist/**"
    }
  },
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/[...all]" }
  ]
}
